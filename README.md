# Module-build
Academic project

# main.py
The main program that starts the quiz, imports modules, and displays the final result.
# quiz_module.py
Contains quiz questions and the function responsible for running the quiz.
# score.py
Contains a reusable function that calculates and displays the user's performance.
